
 /**
  * Enum to define different protection formats
  */
export enum ProtectionFormat {
  /**
   * Flat JSON JWS format
   */
  FlatJsonJws = 0,

  /**
   * Format for a DID authentication request
   */
  DidAuthentication,
}
